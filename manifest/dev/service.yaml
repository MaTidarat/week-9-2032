apiVersion: v1
kind: Service
metadata:
  name: tcc-nodejs-service
  namespace: group-3
spec:
  selector:
    app: tcc-nodejs
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP